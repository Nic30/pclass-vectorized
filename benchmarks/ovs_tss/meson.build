
# the ovs subdir has to be first generated by the ovs_prepare.sh
subdir('ovs')

srcs = ['table_lookup.cpp',]

benchmark_exe = executable('table_lookup',
    srcs,
    dependencies: [pcv_dep, ovs_dep],
    cpp_args: EXTRA_CXX_ARGS + ['-O3'],
    include_directories : ['./ovs/', './ovs/include']
    #c_args: EXTRA_C_ARGS,
)
test('n_tree_lookup', benchmark_exe, workdir : meson.source_root(),
	args : ['tests/data/acl1_100', '4096', '10000000', '0'])